# ── Ingest only (run locally, not deployed) ────────────────────────────────────
# Install with: pip install -r requirements-ingest.txt
torch==2.3.1
sentence-transformers==3.0.1
transformers==4.41.2
pdfplumber==0.11.0
chromadb==0.5.3
